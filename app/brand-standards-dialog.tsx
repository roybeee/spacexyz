'use client';
import {useEffect,useMemo,useRef,useState} from 'react';
import {Store,Plus,Copy,Save,Trash2,ArrowRight,RefreshCw,ExternalLink} from 'lucide-react';
import {Dialog,DialogContent,DialogHeader,DialogTitle,DialogDescription} from '@/components/ui/dialog';
import {Tabs,TabsList,TabsTrigger} from '@/components/ui/tabs';
import {AlertDialog,AlertDialogContent,AlertDialogHeader,AlertDialogTitle,AlertDialogDescription,AlertDialogFooter,AlertDialogCancel,AlertDialogAction} from '@/components/ui/alert-dialog';
import {brandRoles,brandRoleNames,moduleNames,ofdBrand,validateBrand,brandScene,brandReplacementItems,type BrandRole,type BrandStandard,type BrandRow} from '@/lib/brand-standards';
import {materialProduct,productFinish} from '@/lib/material-products';
import {materials,type SceneData} from '@/lib/scene-model';
import {randomId} from '@/lib/random-id';
import ProductMaterialDialog from './product-material-dialog';
import './brand-standards.css';

class BrandError extends Error{constructor(message:string,public status?:number){super(message)}}
async function api<T=unknown>(url:string,options?:RequestInit):Promise<T>{const r=await fetch(url,options),data=await r.json().catch(()=>null);if(!r.ok)throw new BrandError(data&&typeof data==='object'&&'error' in data&&typeof data.error==='string'?data.error:'브랜드 저장소에 연결하지 못했습니다.',r.status);if(!data)throw new BrandError('저장 결과를 확인하지 못했습니다.');return data as T}
function checkedRow(v:BrandRow):BrandRow{if(!v.id||!Number.isInteger(v.revision)||v.revision<1)throw new Error('브랜드 응답을 확인하지 못했습니다.');return {...v,standard:validateBrand(v.standard)}}
export function BrandPlan({scene}:{scene:SceneData}){const {width:w,depth:d}=scene.room;return <svg className="brand-plan" viewBox={`-350 -350 ${w+700} ${d+700}`} role="img" aria-label={`브랜드 배치 평면 미리보기, 가로 ${w}mm 세로 ${d}mm`}><rect width={w} height={d} fill="#f2eee5" stroke="#6c7581" strokeWidth="40"/>{scene.nodes.filter(n=>!n.hidden).map(n=>{const x=n.host==='left'?0:n.host==='right'?w:n.x+w/2,z=n.host==='front'?d:n.host==='back'?0:n.z+d/2,rotation=n.host==='left'||n.host==='right'?90:-n.rotation;return <g key={n.id} transform={`translate(${x} ${z}) rotate(${rotation})`}><rect x={-n.width/2} y={-n.depth/2} width={n.width} height={n.depth} fill={n.finish?.color??materials.find(m=>m.id===n.material)?.color??'#abb1bc'} stroke="#3c4d62" strokeWidth="16"/><title>{n.name} · {n.width} × {n.depth}mm</title></g>})}<text x={w/2} y={-100} textAnchor="middle" fontSize="150" fill="#586273">{w.toLocaleString()} mm</text><text x={w/2} y={d+230} textAnchor="middle" fontSize="150" fill="#586273">입구 · {d.toLocaleString()} mm 깊이</text></svg>}

export default function BrandStandardsDialog({scene,onClose,onPreview}:{scene:SceneData;onClose:()=>void;onPreview:(candidate:SceneData,title:string,details:string)=>void}){
 const [items,setItems]=useState<BrandRow[]>([]),[standard,setStandard]=useState<BrandStandard>(ofdBrand),[record,setRecord]=useState<{id:string;revision:number}>({id:'ofd-starter',revision:0}),[saved,setSaved]=useState(()=>JSON.stringify(ofdBrand())),[tab,setTab]=useState('standards'),[query,setQuery]=useState(''),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState(''),[picker,setPicker]=useState<BrandRole|null>(null),[pending,setPending]=useState(false),[confirm,setConfirm]=useState<{title:string;description:string;action:()=>void}|null>(null),[room,setRoom]=useState({width:scene.room.width,depth:scene.room.depth,height:scene.room.height}),[mode,setMode]=useState<'surfaces'|'layout'>('layout'),[replace,setReplace]=useState(false);
 const flight=useRef(false),mounted=useRef(true),saveAttempt=useRef<{id:string;revision:number;standard:BrandStandard}|null>(null),loadSequence=useRef(0);
 const dirty=JSON.stringify(standard)!==saved,replacementItems=brandReplacementItems(scene),needsReplacement=replacementItems.length>0;
 async function load(signal?:AbortSignal){const seq=++loadSequence.current;setLoading(true);try{const data=await api<{brands:BrandRow[]}>('/api/brands',{signal});if(mounted.current&&seq===loadSequence.current){setItems(data.brands.map(checkedRow));setError('');}}catch(e){if(mounted.current&&seq===loadSequence.current&&!(e instanceof Error&&e.name==='AbortError'))setError((e as Error).message+' OFD 초안은 바로 편집·미리보기할 수 있습니다.');}finally{if(mounted.current&&seq===loadSequence.current)setLoading(false)}}
 useEffect(()=>{mounted.current=true;const abort=new AbortController();void load(abort.signal);return()=>{mounted.current=false;abort.abort()}},[]);
 function choose(s:BrandStandard,id='ofd-starter',revision=0){const action=()=>{setStandard(structuredClone(s));setRecord({id,revision});setSaved(JSON.stringify(s));setNotice('');setError('');setPending(false);saveAttempt.current=null;setConfirm(null)};if(dirty||pending)setConfirm({title:'편집 중인 내용을 닫을까요?',description:'저장하지 않은 브랜드 변경은 사라집니다. 저장된 브랜드와 현재 도면은 유지됩니다.',action});else action()}
 function close(){if(busy)return;if(dirty||pending)setConfirm({title:'브랜드 편집을 닫을까요?',description:'저장하지 않은 변경은 사라집니다. 저장 결과를 확인하지 못했다면 먼저 같은 요청을 다시 확인하세요.',action:onClose});else onClose()}
 function patch(value:Partial<BrandStandard>){setStandard(s=>({...s,...value}));setNotice('')}
 function updateSlot(role:BrandRole,value:Partial<BrandStandard['materials'][BrandRole]>){patch({materials:{...standard.materials,[role]:{...standard.materials[role],...value}}})}
 async function save(copy=false){if(flight.current)return;let payload=saveAttempt.current;try{if(!payload){payload={id:copy||record.revision===0?randomId():record.id,revision:copy?0:record.revision,standard:validateBrand(standard)};saveAttempt.current=payload;}}catch{setError('이름, 소재 설정, 모듈 치수 범위를 확인하세요.');return}
  flight.current=true;setBusy(true);setError('');try{const result=checkedRow(await api<BrandRow>('/api/brands',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}));if(!mounted.current)return;loadSequence.current++;setLoading(false);setItems(list=>[result,...list.filter(i=>i.id!==result.id)]);setStandard(result.standard);setRecord({id:result.id,revision:result.revision});setSaved(JSON.stringify(result.standard));setPending(false);saveAttempt.current=null;setNotice(`내 계정에 저장됨 · v${result.revision}`);}catch(e){if(!mounted.current)return;if(e instanceof BrandError&&e.status&&e.status<500&&e.status!==408){saveAttempt.current=null;setPending(false);}else setPending(true);setError((e as Error).message);}finally{flight.current=false;if(mounted.current)setBusy(false)}}
 function remove(){setConfirm({title:`‘${standard.name}’을 삭제할까요?`,description:'브랜드 보관함에서 삭제합니다. 이미 적용한 프로젝트의 소재와 배치는 유지됩니다.',action:()=>void (async()=>{if(flight.current)return;flight.current=true;loadSequence.current++;setLoading(false);setBusy(true);try{await api(`/api/brands?id=${record.id}&revision=${record.revision}`,{method:'DELETE'});setItems(list=>list.filter(i=>i.id!==record.id));const s=ofdBrand();setStandard(s);setSaved(JSON.stringify(s));setRecord({id:'ofd-starter',revision:0});setConfirm(null);setError('');setNotice('브랜드를 삭제했습니다.');}catch(e){setError((e as Error).message);setConfirm(null)}finally{flight.current=false;if(mounted.current)setBusy(false)}})()})}
 const planned=useMemo(()=>{try{return {scene:brandScene(scene,standard,{id:record.id,revision:dirty?0:record.revision},mode,room,true),error:''}}catch(e){return {scene:null,error:e instanceof Error&&e.name!=='ZodError'?e.message:'상가·모듈 치수와 소재 설정을 확인하세요.'}}},[scene,standard,record,dirty,mode,room]);
 function preview(){if(!planned.scene)return;if(mode==='layout'&&needsReplacement&&!replace){setError('기존 배치를 빈 상가 시안으로 교체하는 항목에 동의하세요.');return}onPreview(planned.scene,`${standard.name} · ${mode==='layout'?'빈 상가 배치':'마감 적용'}`,`${mode==='layout'?`${room.width.toLocaleString()} × ${room.depth.toLocaleString()} mm · 모듈 요소 ${planned.scene.nodes.length}개 · ${replacementItems.length?replacementItems.join(' · ')+' 교체·초기화':'빈 상가에 새 배치'}`:'바닥과 벽 5면 변경 · 기존 가구 유지'} · ${record.revision&&!dirty?'보관본 v'+record.revision:'저장 전 초안'} · ${standard.status==='draft'?'브랜드 검토 필요':'사용자 검토 완료'}`)}
 return <><Dialog open={!picker&&!confirm} onOpenChange={o=>!o&&!picker&&!confirm&&close()}><DialogContent className="brand-dialog" showCloseButton={!busy}><DialogHeader><DialogTitle><Store size={21}/>브랜드 스튜디오</DialogTitle><DialogDescription>브랜드의 마감 기준과 가구 모듈을 보관하고 매장에 빠르게 적용하세요.</DialogDescription></DialogHeader>
 <div className="brand-workspace"><aside className="brand-sidebar"><div className="brand-side-label">브랜드별 관리 <button aria-label="브랜드 목록 새로고침" disabled={busy||pending} onClick={()=>void load()}><RefreshCw size={14}/></button></div><input className="text-input" aria-label="브랜드 검색" placeholder="브랜드 검색" value={query} onChange={e=>setQuery(e.target.value)}/><button className={`brand-list-item ${record.id==='ofd-starter'?'active':''}`} disabled={busy} onClick={()=>choose(ofdBrand())}><b>올드페리도넛</b><small>편집용 기본 초안</small></button>{items.filter(r=>r.standard.name.toLowerCase().includes(query.toLowerCase())).map(r=><button key={r.id} disabled={busy} className={`brand-list-item ${record.id===r.id?'active':''}`} onClick={()=>choose(r.standard,r.id,r.revision)}><b>{r.standard.name}</b><small>v{r.revision} · {r.standard.status==='draft'?'검토 중':'사용자 검토 완료'}</small></button>)}{loading&&<p className="fineprint">보관한 브랜드 불러오는 중…</p>}<button className="outline-button full" disabled={busy||pending} onClick={()=>{const s=ofdBrand();s.name='새 브랜드';s.note='브랜드의 소재·배치 기준을 입력하세요. 초기 소재와 치수는 제안값입니다.';s.referenceUrl='';choose(s,'new-brand')}}><Plus size={15}/>새 브랜드</button><p className="fineprint">내 계정에 최대 50개<br/>브랜드 저장과 프로젝트 저장은 별개입니다.</p></aside>
 <section className="brand-main"><div className="brand-title-row"><div><h2>{standard.name}</h2><span className="brand-badge">{standard.status==='draft'?'디자이너 초안 · 검토 필요':'사용자 검토 완료'}</span><small>{dirty?'저장하지 않은 변경':record.revision?`보관본 v${record.revision}`:'아직 보관하지 않은 초안'}</small></div><button className="primary-button" disabled={busy} onClick={()=>void save()}><Save size={15}/>{busy?'저장 확인 중…':pending?'같은 저장 다시 확인':'브랜드 저장'}</button></div>
 {error&&<p className="brand-error" role="alert">{error}</p>}{notice&&<p className="brand-notice" role="status">{notice}</p>}{pending&&<p className="brand-error">저장 결과가 불확실합니다. 입력은 유지되며, 같은 저장 요청을 다시 확인할 수 있습니다.</p>}
 <Tabs value={tab} onValueChange={setTab}><TabsList><TabsTrigger value="standards">1. 표준 소재·모듈</TabsTrigger><TabsTrigger value="apply">2. 상가에 적용</TabsTrigger></TabsList></Tabs>
 {tab==='standards'?<fieldset disabled={busy||pending} className="brand-fields"><div className="brand-info"><label className="field-label">브랜드 이름<input className="text-input" value={standard.name} maxLength={80} onChange={e=>patch({name:e.target.value})}/></label><label className="field-label">검토 상태<select className="text-input" value={standard.status} onChange={e=>patch({status:e.target.value as BrandStandard['status']})}><option value="draft">디자이너 초안</option><option value="reviewed">사용자 검토 완료</option></select></label><label className="field-label brand-wide">기준·주의사항<textarea className="text-input" rows={2} maxLength={1000} value={standard.note} onChange={e=>patch({note:e.target.value})}/></label><label className="field-label brand-wide">브랜드 참고 주소<input className="text-input" placeholder="https://" value={standard.referenceUrl} maxLength={500} onChange={e=>patch({referenceUrl:e.target.value})}/></label></div>
 <div className="brand-section-title"><h3>표준 소재 7개 부위</h3><span>제품 이미지를 눌러 코드 변경</span></div><div className="brand-materials">{brandRoles.map(role=>{const s=standard.materials[role],p=materialProduct(s.finish.catalogId);return <article className="brand-material" key={role}><button className="brand-swatch" onClick={()=>setPicker(role)} aria-label={`${brandRoleNames[role]} 제품 변경`} style={{background:s.finish.color??'#e6e0d4'}}>{p?.image?<img src={p.image} alt={`${p.brand} ${p.code}`} loading="lazy"/>:<span>{s.finish.color}</span>}</button><div><h4>{brandRoleNames[role]}</h4><b>{p?`${p.brand} · ${p.code}`:'조색 코드 미지정'}</b><small>{p?.name??'시각화용 제안 색상'}</small><button className="text-button" onClick={()=>setPicker(role)}>제품 선택</button>{p&&<a href={p.sourceUrl} target="_blank" rel="noreferrer" className="text-button">제조사 <ExternalLink size={11}/></a>}<label className="brand-color"><input type="color" aria-label={`${brandRoleNames[role]} 제안 색상`} value={s.finish.color??'#eeeeee'} onChange={e=>updateSlot(role,{material:role==='metal'?'steel':'plaster',finish:{color:e.target.value},note:'직접 지정한 시각화 색상 · 제조사 조색 코드 미지정.'})}/>직접 색상 지정</label></div><label className="field-label brand-wide">적용 기준<textarea className="text-input" rows={2} aria-label={`${brandRoleNames[role]} 적용 기준`} maxLength={300} value={s.note} onChange={e=>updateSlot(role,{note:e.target.value})}/></label></article>})}</div>
 <div className="brand-section-title"><h3>배치 모듈</h3><span>치수 단위 mm · 실제 설비 별도</span></div><div className="brand-modules">{(Object.keys(moduleNames) as (keyof typeof moduleNames)[]).map(key=>{const m=standard.modules[key];const edit=(v:Partial<typeof m>)=>patch({modules:{...standard.modules,[key]:{...m,...v}}});return <article key={key}><label className="brand-toggle"><input type="checkbox" checked={m.enabled} onChange={e=>edit({enabled:e.target.checked})}/><b>{moduleNames[key]}</b></label><div className="brand-dimensions">{([['width',key==='seating'?'벤치 길이':'가로',600,5000],['height','높이',400,2400],['depth','깊이',300,1200]] as const).map(([prop,label,min,max])=><label key={prop}>{label}<input type="number" aria-label={`${moduleNames[key]} ${label}`} className="text-input" min={min} max={max} step={10} value={m[prop]} onChange={e=>edit({[prop]:Number(e.target.value)})}/></label>)}</div><textarea className="text-input" aria-label={`${moduleNames[key]} 기준 메모`} rows={2} value={m.note} maxLength={300} onChange={e=>edit({note:e.target.value})}/></article>})}</div><div className="brand-manage-actions"><button className="outline-button" onClick={()=>void save(true)}><Copy size={15}/>다른 브랜드로 복제 저장</button><button className="text-button" disabled={!record.revision} onClick={remove}><Trash2 size={14}/>이 브랜드 삭제</button></div><p className="fineprint">‘사용자 검토 완료’는 본사 공식 인증을 뜻하지 않습니다. 제품 채택·수량·시공 적합성은 별도로 확인하세요.</p></fieldset>:<div className="brand-apply"><div><h3>상가 조건</h3><label className="field-label">적용 방법<select className="text-input" aria-label="브랜드 적용 방법" value={mode} onChange={e=>{setMode(e.target.value as typeof mode);setReplace(false)}}><option value="layout">빈 상가에 소재 + 모듈 배치</option><option value="surfaces">현재 공간의 바닥·벽만 교체</option></select></label>{mode==='layout'&&<><div className="brand-room-presets">{[[5000,6600,'약 10평'],[6000,8300,'약 15평'],[7200,9200,'약 20평']].map(([w,d,label])=><button className="outline-button" key={label} onClick={()=>setRoom(r=>({...r,width:Number(w),depth:Number(d)}))}>{label}</button>)}</div><div className="brand-dimensions">{([['width','가로',2400,20000],['depth','세로',2400,20000],['height','층고',2200,6000]] as const).map(([key,label,min,max])=><label key={key}>{label} mm<input className="text-input" type="number" aria-label={`상가 ${label}`} value={room[key]} min={min} max={max} onChange={e=>setRoom({...room,[key]:Number(e.target.value)})}/></label>)}</div><p>{(room.width*room.depth/1e6).toFixed(2)} ㎡ · 약 {(room.width*room.depth/3305785).toFixed(1)}평</p><p className="fineprint">직사각형 계획 치수입니다. 실제 벽·기둥·설비·출입 조건은 실측 후 조정하세요.</p>{needsReplacement&&<label className="brand-replace"><input type="checkbox" checked={replace} onChange={e=>setReplace(e.target.checked)}/><span>{replacementItems.join(' · ')}을 새 배치로 교체·초기화합니다. 적용 후 실행 취소할 수 있습니다.</span></label>}</>}<div className="brand-apply-note"><b>{standard.status==='draft'?'검토 중인 디자인 초안':'사용자가 검토한 기준'}</b><p>{standard.note}</p>{standard.referenceUrl.startsWith('https://')&&<a href={standard.referenceUrl} target="_blank" rel="noreferrer">브랜드 참고 페이지 <ExternalLink size={12}/></a>}</div></div><div className="brand-plan-panel">{planned.scene?<BrandPlan scene={planned.scene}/>:<div className="brand-plan-error" role="alert">{planned.error}</div>}<p>배치 검토용 평면 · 소재 결·실제 시공 치수는 3D와 도면에서 확인</p><button className="primary-button full" disabled={busy||pending||!planned.scene||(mode==='layout'&&needsReplacement&&!replace)} onClick={preview}>이 구성으로 미리보기 <ArrowRight size={16}/></button><p className="fineprint">미리보기의 ‘이대로 적용’을 눌러야 현재 디자인이 변경됩니다.</p></div></div>}
 </section></div></DialogContent></Dialog>{picker&&<ProductMaterialDialog selectionOnly shortlist={[]} onShortlistChange={()=>false} currentId={standard.materials[picker].finish.catalogId} target={`${standard.name} · ${brandRoleNames[picker]}`} onClose={()=>setPicker(null)} onApply={async p=>{updateSlot(picker,{material:p.base,finish:productFinish(p),note:`${p.brand} ${p.code} ${p.name} 제안 · 적용 사양과 실물 샘플 확인.`});return true}}/>}<AlertDialog open={!!confirm} onOpenChange={open=>!open&&!busy&&setConfirm(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>{confirm?.title}</AlertDialogTitle><AlertDialogDescription>{confirm?.description}</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel disabled={busy}>취소</AlertDialogCancel><AlertDialogAction disabled={busy} onClick={e=>{e.preventDefault();confirm?.action()}}>계속</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></>;
}
