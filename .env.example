# Optional: enable AI material and scene command proposals.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

OPENAI_IMAGE_MODEL=gpt-image-2.5-sunburst-2026-09-08

# Server-side AES-256-GCM key for per-owner Hermes connections (base64, 32 bytes).
# Generate once; do not rotate without migrating stored connections.
SPATIAL_CONNECTION_KEY=
