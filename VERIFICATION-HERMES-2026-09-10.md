# Hermes 연결 구현 및 검증 — 2026-09-10

요청: Orbit처럼 기존 Hermes를 SPATIAL의 AI 분석·편집에 연결.

## 구현

- Orbit의 `roybeee/ORBIT` 공개 소스와 Hermes 공식 API Server 문서를 확인해 native `/v1/runs` 방식을 적용.
- 사용자별 HTTPS 주소·연결 암호 등록, 인증된 기능 조회, 비인증 접근 차단 확인, 연결 해제.
- AES-256-GCM 암호 보관 및 사용자 ID를 인증 데이터로 결합. 암호 반환·프로젝트 포함·Git 저장 없음.
- 운영 서버에 별도 `SPATIAL_CONNECTION_KEY` 비밀값 설정. 실제 Hermes 주소와 암호는 아직 등록하지 않음.
- 문장 편집, 매장 사진 초안, 물체 사진 분석에 공통 Hermes 어댑터 적용. 사진은 native 이미지 입력으로 전달.
- D1에 실행 식별자·입력 해시·상태·결과 보관, 접수 유실 시 같은 중복 방지 키로 재확인. 입력 사진과 전체 요청 본문은 이 테이블에 저장하지 않음.
- 최근 요청 상태 조회·중지, 원격 종료 확인, 취소 결과 미적용, 다른 계정의 조회·중지 차단.
- Hermes와 OpenAI 이미지 생성의 준비 상태 구분. 이미지 생성은 기존 OpenAI 경로 유지.
- Drizzle 0005 마이그레이션 추가. 기존 마이그레이션 수정 없음.

## 확인 결과

- 전체 **50개 테스트 파일 / 679개 검사 통과**.
- 새 검사 20개: 실제 라우트·SQLite·암호화 경계 16개, 클라이언트 요청 복구 4개. 외부 Hermes 응답 및 테스트 신원은 모의 경계.
- TypeScript 검사 통과.
- Sites 배포용 빌드 성공. `/api/hermes`, `/api/hermes/jobs` 및 기존 API 포함 확인.
- 브라우저: AI 연결 설정 열기, Hermes 우선 입력 화면, 빈 입력 시 저장 버튼 비활성화, 입력 후 활성화, 비로그인 저장 거절, 입력 유지, 닫기 확인. 설정 화면 스크린샷에서 배치 확인.
- 소스 내 실제 GitHub PAT/OpenAI 키/개인키 패턴 검사: 발견 없음.

## 실제 검증 범위의 한계

- 실제 Hermes 주소와 연결 암호를 받지 않았으므로 실제 게이트웨이 접속·모델 응답·사진 분석 품질은 검증하지 않음. Orbit에 저장된 암호는 복호화·복사하지 않음.
- 테스트 브라우저는 사이트 사용자 로그인 상태가 아니므로 연결 저장 성공은 모의 신원을 사용하는 실제 API 테스트로 확인. 인증 우회 없음.
- 테스트 브라우저의 WebGL 사용 불가 상태는 이전과 같음. 이번 변경의 UI는 확인했으나 브라우저에서 사진 분석부터 3D 렌더까지의 전체 경로는 검증하지 않음.
- 접수 번호가 없는 불확실 요청의 재시도 기한이 지나면 자동 복구하지 않음. Hermes에서 확인 필요. 탭·기기 변경 시 원래 입력과 재확인 식별자의 자동 복구는 보장하지 않음.
- Hermes 네이티브 도구 권한은 기존 게이트웨이 설정을 따름. SPATIAL은 JSON 제안만 요청하며 게이트웨이 권한을 변경하거나 도구 승인을 자동 처리하지 않음.

이 문서는 소스·빌드·검증 시점의 기록입니다. 배포 성공 여부는 해당 소스 버전의 Sites 배포 상태로 별도 확인합니다.
