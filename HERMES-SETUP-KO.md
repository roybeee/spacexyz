# SPATIAL에 기존 Hermes 연결하기

Orbit과 같은 Hermes native runs 방식을 사용합니다. 새로운 모델 키를 SPATIAL에 발급할 필요 없이 기존 Hermes의 모델과 공급자 설정을 사용합니다.

1. 기존 Hermes 게이트웨이를 켭니다. Orbit에서 사용 중인 주소라면 게이트웨이의 API 설정을 다시 변경할 필요가 없습니다.
2. SPATIAL에 로그인하고 상단 **AI 연결 설정**을 엽니다.
3. Orbit의 연결 화면에 등록했던 **HTTPS 주소**와 해당 프로필의 **연결 암호(API_SERVER_KEY)**를 넣습니다. `/p/프로필명`이 있으면 포함합니다. 끝의 `/v1`은 자동 정리합니다. 로컬 IP와 localhost는 호스팅 서버에서 사용할 수 없습니다.
4. **Hermes 연결 확인·저장**을 누릅니다. 최신 gateway의 실행·상태·중지·영속 중복 방지 지원과 인증 보호를 확인합니다.
5. **이전 작업으로 돌아가기**에서 사진 분석 또는 문장 편집을 시작합니다. 실제 응답이 성공해야 모델 연결까지 확인된 것입니다.

암호는 GitHub나 프로젝트 JSON에 저장하지 않습니다. 서버의 사용자별 연결 레코드에 AES-256-GCM으로 암호화하며, 본인 계정만 사용할 수 있습니다. Orbit의 암호화된 연결 레코드를 SPATIAL에서 자동으로 가져오는 기능은 없습니다. 대화에 암호를 다시 올릴 필요 없이 앱 입력란에 직접 등록하면 됩니다.

## 가능한 작업

- 한국어 문장 → 공간 편집 제안 → 사용자 검토 후 적용.
- 매장 사진 → 입력한 공간 치수를 기준으로 편집 가능한 추정 배치.
- 물체 사진 → 사용자 치수에 맞출 기본형/부품 분석 → 도면에 배치.
- 장시간 요청 상태 확인, 같은 요청 재확인, 중지와 종료 확인.

사진 분석에는 이미지 입력을 지원하는 Hermes와 모델이 필요합니다. 단일 사진에서 정확한 실측이나 보이지 않는 구조를 복원하지 않습니다. 3D 장면의 완성 이미지 생성은 별도 OpenAI 연결이 필요한 기존 기능입니다. Hermes 도구로 생성한 파일을 이미지 시안으로 가져오는 경로는 이번 연결에 포함하지 않습니다.

## 재연결과 중지

요청 중 연결이 끊기면 사진·입력·장면을 유지하고 같은 기능을 다시 요청하세요. 이 탭에 남은 요청 식별자와 서버 접수 기록으로 같은 실행을 확인합니다. 입력이 달라지거나 완료 후 새로 요청하면 별도 실행입니다. 브라우저 탭을 닫거나 다른 기기로 이동하면 이전 입력과 요청 식별자의 자동 복구는 보장하지 않습니다. 서버의 최근 요청 목록은 유지됩니다.

**최근 Hermes 요청 → 상태 확인**은 원격 상태를 조회합니다. **중지**는 Hermes에 중지를 전달하고 종료 상태를 확인합니다. 버튼 클릭 자체가 즉시 종료를 뜻하지 않습니다. 승인 대기는 Hermes에서 확인하거나 중지할 수 있습니다. SPATIAL은 네이티브 도구 승인 권한을 자동 행사하지 않습니다.

접수 응답을 받기 전에 통신이 끊겨 실행 번호가 없으면 원래 입력으로 재요청해 같은 접수를 찾습니다. 접수 재시도 기간(최대 약 9분 30초)이 지났으면 중복 실행 방지를 위해 재전송하지 않습니다. 이 경우 Hermes에서 기존 실행 상태를 직접 확인해야 하며 앱에서 결과 복구·중지는 제한됩니다. 아직 해결되지 않은 요청은 연결 해제를 막습니다. 같은 주소의 연결 암호 갱신은 가능합니다.

Hermes 프로필의 실제 도구 권한은 기존 게이트웨이 설정을 따릅니다. SPATIAL의 분석 지시는 외부 도구 실행 대신 JSON 제안만 요구합니다. 이는 Hermes 자체 권한 설정을 변경하는 기능이 아닙니다. 다른 앱이 쓰는 프로필 설정을 SPATIAL이 자동 변경하지 않습니다.

## 개발 및 검증

- `lib/hermes-server.ts`: 암호 보관, 기능 확인, native runs, 접수 영속화·조회·중지.
- `app/api/hermes`, `app/api/hermes/jobs`: 소유자 인증과 같은 출처 검사.
- `lib/ai-client.ts`: 동일 입력 재확인. 브라우저에는 본문·사진·암호 대신 해시와 요청 번호만 임시 색인으로 저장합니다.
- `tests/hermes.test.mjs`: 실제 API 라우트·SQLite·AES-GCM을 사용하고 외부 게이트웨이와 신원 경계만 대체합니다.
- `tests/ai-client.test.mjs`: 202 반복 조회, 응답 유실, 취소 후 복구, 새 요청 처리.

참조: [Orbit의 Hermes 연결 안내](https://github.com/roybeee/ORBIT/blob/main/docs/Hermes_Setup.ko.md), [Hermes API Server 공식 계약](https://hermes-agent.nousresearch.com/docs/user-guide/features/api-server).
