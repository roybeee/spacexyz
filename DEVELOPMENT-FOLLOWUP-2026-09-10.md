# SPATIAL AI 후속 개발 및 검증 · 2026-09-10

기존 한 시간 개발 뒤 남아 있던 검증과 오류 복구를 이어서 진행했습니다. 이 기록은 앞선 개발 보고서의 브라우저 미검증 상태를 아래 범위에서 갱신합니다.

## 이번 수정

- 공간 크기를 바꾼 뒤 사진 초안을 새로 열면 현재 치수가 반영됩니다. AI 연결 설정을 경유했다 돌아오면 초안에 입력하던 치수·브랜드를 유지합니다. 새로 시작한 초안에는 이전 입력이 남지 않습니다.
- 사진·프로젝트·장면이 바뀐 뒤 도착한 사진 분석 결과를 적용하지 않습니다. 닫힌 초안 요청을 취소하고 중복 실행을 차단합니다. 적용 전 제안에도 원본 장면 검사를 수행합니다.
- 서버에 키가 있다는 사실을 AI 연결 성공으로 표시하지 않습니다. 설정 조회 실패, 로그인 필요, 키 입력 전 상태를 구분합니다. API 키는 서버 응답에 노출하지 않습니다.
- AI가 잘못된 편집 명령을 반환하면 사용자 입력 오류가 아닌 AI 응답 오류로 안내합니다.
- AI 이미지 요청마다 서버에 원자적으로 요청 번호를 기록합니다. 같은 번호의 동시 요청·응답 소실·타임아웃·잘못된 출력·저장 실패 뒤 재시도는 추가 이미지 생성을 호출하지 않습니다. 다른 내용을 같은 번호로 보내는 요청도 거절합니다.
- 이미지 창은 실패한 요청의 입력과 번호를 유지합니다. **같은 요청 결과 확인**으로 이전 요청을 조회하고, **새 요청으로 시작**을 명시적으로 선택해야 새 생성을 진행합니다. 새 요청의 별도 비용 가능성을 안내합니다. 클라이언트 요청 복구 정보는 현재 페이지 메모리에만 유지됩니다.
- 이미지 메타데이터 저장 응답을 놓쳐도 이미 저장된 파일을 삭제하지 않습니다. DB 상태까지 확인할 수 없으면 파일과 요청 기록을 보존합니다. 이 경우 자동 이미지 재생성이나 결과 복구를 보장하지 않습니다.
- 같은 창에서 연속 생성한 시안을 프로젝트에 연결할 때 이전 시안 목록의 변화가 장면 변경으로 오인되지 않도록 수정했습니다.
- crypto.randomUUID가 없는 브라우저에서도 암호학적 난수 기반 UUID로 가구·템플릿·레이어·세트를 생성합니다.
- 소재 응답·Three 소재 인덱스 타입을 정리하고, 기존 BigInt 사용에 맞춰 TypeScript 대상을 ES2020으로 정리했습니다.

## 확인한 범위

| 검증 | 결과 |
|---|---|
| 자동 회귀 검사 | 44개 파일, 617개 검사 통과. AI 제공자는 모의 응답 사용. |
| 이미지 요청 중복 방지 | 실제 SQLite에 전체 마이그레이션 적용. 최초 조회가 동시에 비어 있는 경합, 완료 결과 재조회, 소유자 격리, 입력 충돌, 요청 기록 실패, 이미지/DB 저장 실패, 타임아웃 검증. |
| 초안 치수 | 공간 폭 8,400mm로 변경 후 새 초안에 반영 확인. 초안 9,000mm 입력 후 설정 복귀에서도 유지 확인. |
| 템플릿 적용 | 9,000 × 6,400mm 초안 생성 → 적용 전 제안 → 적용 → 면적 57.6m² 확인. |
| 소재 편집 | 바닥 딥 월넛 적용 → 복사 → 안쪽 벽 붙이기 → 실행 취소로 웜 화이트 복원 확인. 3D 픽셀 변화는 확인하지 못함. |
| 기능 검색 | JSON 키워드로 해당 기능 하나를 찾아 실행 확인. |
| 평면도 | A3 1:50, 9,000 × 6,400mm 치수와 13개 요소 목록 화면 표시 확인. |
| 온라인 저장 | 로그인 없는 검증 환경에서 로그인 필요 오류 안내 확인. 인증된 실제 저장 성공은 미검증. |
| JSON 파일 다운로드 | 기능 실행 후 다운로드 완료 이벤트가 잡히지 않아 파일 수신 성공은 미검증. |

## 남은 검증과 지원 범위

- 배포 환경의 AI 키 설정은 비어 있습니다. 실제 사진 분석·AI 문장 편집·유료 이미지 생성의 성공 호출은 수행하지 않았습니다. 키 설정은 실제 제공자 접근 권한과 별도로 확인해야 합니다.
- 검증용 브라우저의 그래픽 기능이 비활성화되어 WebGL 초기화가 실패했습니다. 따라서 3D 면 클릭·카메라·문 재생·둘러보기·PNG/GLB 브라우저 내보내기는 이 환경에서 검증하지 못했습니다. 계산과 Three 형상/GLB 관련 자동 검사는 통과했습니다.
- SKP/MAX 직접 편집은 아직 지원하지 않습니다. GLB 기반 모델 교환과 편집 가능한 자체 장면이 현재 지원 범위입니다.
- 신규 마이그레이션은 `0004_unusual_liz_osborn.sql`입니다. 기존 0000–0003 파일은 변경하지 않았습니다. 요청 기록에는 API 키와 입력 이미지 원문을 저장하지 않습니다.
