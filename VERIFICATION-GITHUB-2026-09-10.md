# GitHub 업로드 전 동작 검증

검증일: 2026-09-10. 대상: SPATIAL v22.

- 기존 서비스 런타임 소스: `dc1ed72769bad5dbb4722bed8867a682bf8350ab`
- 업로드 대상: `https://github.com/roybeee/spacexyz`
- 이번 변경은 통합 시험과 문서 보강입니다. 서비스의 인증·접근 범위나 AI 설정은 변경하지 않았습니다.

## 확인 결과

| 범위 | 결과 | 근거와 한계 |
|---|---|---|
| 전체 자동 검사 | 통과 | 48개 시험 파일, 659개 검사. 기존 651개와 사진 작업 흐름 8개 포함. |
| TypeScript | 통과 | `tsc --noEmit --incremental false`. |
| 사진 업로드 → 분석 → 부품 생성 | 통합 검사 통과 | 실제 업로드/분석 라우트, 이미지 검사, 부품 생성 로직을 연결. AI 응답·사용자 인증·R2 저장은 시험 대역 사용. |
| 프로젝트 저장 → 재열기 | 통합 검사 통과 | 모든 마이그레이션을 실제 SQLite에 적용. 사진 참조·5개 부품 그룹·제품 마감·디자인 안을 저장하고 다시 읽어 동일성 확인. |
| 가구 세트 저장 → 재사용 | 통합 검사 통과 | 참고사진 유지 및 독립 그룹 생성 확인. |
| 소유권 및 충돌 처리 | 통과 | 다른 사용자의 이미지·프로젝트·세트 접근 거절, 비활성 디자인 참조 검사, 오래된 revision의 덮어쓰기 거절. |
| 실제 제품 브라우저 동작 | 통과 | LX Z:IN BM013 검색, 실제 550×550 이미지 로딩, 바닥 적용, 왼쪽 벽 붙이기, 실행 취소로 이전 소재 복원. |
| 사진 가구 브라우저 입력 | 통과 | 1,400×800×780mm 입력과 개념도 갱신. 치수 변경 시 실측 체크 해제. 업로드 창 및 로그인 필요 안내. |
| 배포 서버 설정 조회 | HTTP 200 | `/api/config` 응답. `aiKeyConfigured:false`, `signedIn:false`. |
| 배포 서버의 이미지/프로젝트 접근 | HTTP 401 | 현재 검증 세션에 사용자 로그인이 없어 보호된 API가 요청을 거절함. 성공적인 인증 사용자 작업으로 간주하지 않음. |
| 3D 형상 및 GLB | 자동 검사 통과 | 실제 Three 형상·GLB 직렬화와 그룹 계층/치수 검증. 브라우저의 최종 픽셀은 아래 환경 제한으로 미검증. |

## 이번에 추가한 통합 시험

`tests/photo-workflow.test.mjs`는 개별 함수 시험을 넘어 다음 흐름을 연결합니다.

1. 실제 PNG 바이트 업로드, 소유자별 메타데이터와 이미지 바이트 저장.
2. AI 키 누락 시 이미지 보존 및 수동 기본형 생성.
3. 사진 분석 응답에서 5개 편집 부품을 생성하고 입력한 전체 크기 유지.
4. 실제 제조사 마감재를 적용하고 사진·부품·디자인 안을 프로젝트에 저장·재조회.
5. 가구 세트 저장·읽기·회전 재배치와 독립 그룹 생성.
6. 오래된 저장 버전의 덮어쓰기 거절.
7. 다른 계정의 읽기·분석·저장·세트 재사용 차단.
8. 사진이 비활성 디자인 안에만 남아 있어도 소유권 검사 유지.

이 시험은 외부 AI의 실제 사진 이해 품질이나 실제 로그인 세션을 입증하지 않습니다. 운영 데이터는 변경하지 않았습니다.

## 아직 완료하지 못한 검증

- 서버의 AI 연결 키가 비어 있어 실제 유료 사진 분석·이미지 생성 호출은 실행하지 않았습니다. GitHub 인증 토큰은 AI API 키로 사용하지 않습니다.
- 검증 브라우저는 그래픽 장치가 비활성화되어 WebGL 컨텍스트를 만들지 못합니다. 브라우저 3D 면 클릭·카메라 조작·렌더 결과의 시각적 정확성은 미검증입니다.
- 검증 브라우저와 배포 서버 요청에는 로그인된 사용자 식별 정보가 없어 실제 계정의 업로드→분석→저장 전체 흐름은 완료하지 못했습니다. 인증을 제거하거나 사용자 헤더를 위조하지 않았습니다.
- 위 제한은 시험용 인증/AI 응답으로 통과시킨 결과와 구분합니다. 전체 실제 서비스 검증 완료로 표시하지 않습니다.

## 업로드 범위

전체 Git 개발 이력과 애플리케이션·서버 코드, 18개 제품 스와치, DB 스키마/마이그레이션, 의존성 잠금 파일, 48개 시험 파일, 사용 안내와 제품 출처 기록을 포함합니다. 재생성 가능한 `node_modules`/빌드 산출물, 로컬 DB와 사용자 업로드, 실제 환경변수/인증 토큰은 포함하지 않습니다.

기존 이력의 512개 Git blob에 GitHub/OpenAI 토큰 및 개인키 패턴 검사를 수행했으며 해당 패턴을 발견하지 못했습니다. 이는 모든 종류의 비밀값을 자동으로 탐지했다는 보장은 아닙니다. 제공된 토큰은 요청 단위 인증에만 사용하고 커밋·원격 URL·설정 파일에 저장하지 않았습니다.

제조사 이미지의 출처와 재사용 권한 상태는 [MATERIAL-PRODUCT-SOURCES.md](MATERIAL-PRODUCT-SOURCES.md)에 기록되어 있습니다. 제품 코드 확인과 이미지 사용권 취득은 별개이며, 이 저장소에 업로드했다고 제조사 이미지의 오픈소스 이용 허락이 생기는 것은 아닙니다.
