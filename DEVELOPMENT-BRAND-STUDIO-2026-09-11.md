# 브랜드 스튜디오 v23 — 시공매뉴얼 기반 표준 매장 생성 (2026-09-11)

브랜드 스튜디오의 배치 생성기를 박스 모듈 3개에서 **매뉴얼 규칙 엔진**으로 교체했다. 앱의 다른 화면·도구·계약은 바꾸지 않았다(`studio.tsx` 무변경, 다이얼로그 props 동일). 사용자는 상가 치수·출입구 위치·좌석 여부 3가지만 입력하고, 규칙이 배치를 결정한다.

## 변경 파일

| 파일 | 변경 |
|---|---|
| `lib/brand-layout.ts` | **신규.** `planBrandStore(brand, room, options, idFactory)` — 존 띠 계산, 서비스 라인 폭·측벽 L자 길이 탐색, 설비 그룹 생성, 출입구 미러링, 리포트. `fillPlinthModules` 공개. |
| `lib/brand-application.ts` | 리포트 스키마(`brandReportSchema`), 등급 `F/S/O`, 출입구 위치, `brandApplication`에 `entrance / seating / report` 선택 필드 추가. |
| `lib/brand-standards.ts` | 소재 역할 8개(`fabric` 추가), 모듈 7종(`showcase / beverage / pickup / windowBar` 추가), `rules` 블록(동선·출입문·조명·존 비율·현장 확인 항목). 신규 필드는 모두 `.default()`로 하위 호환. `brandScene(…, options)` 7번째 인자 추가, `brandPlan()` 추가. OFD 기본값을 매뉴얼 v1.0으로 교체. |
| `app/brand-standards-dialog.tsx` | 가독성 있는 형식으로 재작성. 탭 순서 “1. 표준 매장 만들기 / 2. 브랜드 표준 편집”. 출입구·좌석 옵션, 리포트 패널(`BrandReportView`), 규칙 편집기, 모듈 등급 배지, 평면에 존 띠·주통로 표시. |
| `app/brand-standards.css` | 등급·상태 배지, 상가 옵션, 리포트, 규칙 편집기 스타일 추가(기존 클래스 유지). |
| `tests/brand-standards.test.mjs` | 구 생성기 값을 고정하던 단언 6개 갱신(아래). 2차: 15·20평 프리셋 요소 수 53/64·그룹 11/13·POS 상판 1,200. |
| `tests/brand-studio.test.mjs` | **신규** 17개 검사. |
| `README.md`, `DEVELOPMENT-BRAND-STANDARDS-2026-09-11.md` | 범위·한계 갱신, 초기 구현 기록임을 표기. |

## 엔진이 적용하는 규칙 (OFD 시공매뉴얼 v1.0 기본값, 브랜드별 편집 가능)

- **고정 사양 [F]**: 진열 플린스 GL+900 · 깊이 800 · 모듈 600/900/1200(폭은 이 세 값만 허용) · 상판 석재 · 몸통 목재 · 콘크리트 기단, 카운터 상판 GL+1,050 · 하부 도어형, 조명 3,000K, 주통로 1,200 · 부통로 900 · 대기열 2,400.
- **존 배분**: 깊이 방향 띠 4개 — 후방(작업대/랙 깊이 + 부통로) → 진열·카운터·픽업 라인(800) → 대기열(2,400) → 객석·여유(잔여). 서비스 라인 폭은 좌→우 [코너 여유][진열 모듈][POS][음료][픽업]. 면적은 진열·카운터·픽업 각각 “라인 폭 × (800 + 대기열)”, 후방 “전폭 × 후방 띠”, 잔여를 Z5로 계산한다(내부 유효면적 기준, 벽 120mm 안쪽 면).
- **진열 20%는 하드 룰**: POS·음료·픽업 폭을 브랜드값과 설계 최소값(900/900/600) 사이에서 8가지 조합으로 탐색해 (1) 진열 ≥ 최소(허용 오차 0.5%p), (2) 카운터·픽업 존 목표 위반 수 최소, (3) 서비스 폭 최대 순으로 고른다. 어떤 조합도 안 되면 생성하지 않고 필요한 폭과 “음료·픽업 모듈 끄기”를 안내한다. 다른 존은 목표 범위 ±1%p 밖이면 `조정 권장`으로 표시하고 개선 조언을 붙인다.
- **출입구**: 전면 벽 좌/중/우. 진열은 출입구 쪽에서 시작하고 좌석은 반대쪽. 우측 출입구는 좌측 계획을 x축 미러링(부호 있는 0 방지). 출입구에서 대기열까지 주통로 폭(1,200)을 비워 두고, 전면 장애물(창가 바·벤치 유닛) 사이 실제 여유 폭을 리포트한다.
- **좌석**: 창가 바(전면, 주통로 제외 구간, 최소 1,200, 스툴 600 간격, 전면 여유 ≥ 900 필요) + 벽면 벤치 유닛(출입구 반대 벽, 테이블당 1,200, 최대 2개, 주통로와 겹치면 생략). 생략 사유는 경고로 남긴다.
- **분류**: 계획 치수(벽 중심선) 기준 평수를 소수 1자리로 반올림해 A ≥ 40 / B 20~40 / C 10~20 / D 10 미만(인스토어 검토 경고). 5,000×6,600 = 10.0평 → Type C.

## 측벽 L자 진열 연장 (2차 커밋)

- **언제**: 배치 규칙 `진열 확장 = 자동`(기본)이고, 직선 라인만으로는 카운터·픽업 존 목표를 못 맞출 때. 10평(5,000×6,600)은 직선으로 충분하므로 결과가 이전과 완전히 같다.
- **어디에**: 출입구 쪽 측벽. 서비스 라인의 코너 모듈에서 이어져 전면 방향으로 달리고, 출입문 앞 진입 구역(주통로 폭과 문폭+200 중 큰 값, 기본 1,200mm) 앞에서 멈춘다. 고객은 문으로 들어와 진열을 따라 카운터로 향한다. 중앙 출입구는 진열 앞 통로(플린스 800 + 주통로 1,200)가 진입 통로와 겹치지 않을 때만 허용한다.
- **면적 계산**: 라인 진열 존(폭 × (800 + 대기열))과 측벽 진열 존(길이 × 2,000)의 **합집합**으로 계산해 이중 계산을 막는다. 대기열 띠 안의 측벽 구간은 이미 라인 진열 존에 포함되므로, 실제로는 대기열 너머 전면 구간만 면적을 더한다. 코너 라인 진열은 진열 앞 통로 폭(2,000) 이상이어야 한다.
- **탐색**: 측벽 길이 후보(1,200부터 600 단위, 최소 1,200) × POS·음료·픽업 폭 후보(설계 최소 → 브랜드 폭 → +600 → +1,200)를 전부 평가해 ① 진열 ≥ 최소(오차 0.5%p) ② 존 목표 위반 수 최소 ③ 브랜드 폭과의 차이 최소 ④ 측벽 길이 최소 ⑤ 진열 면적 최대 순으로 고른다. 따라서 필요할 때만 폭을 줄이거나 늘리고, 필요할 때만 측벽을 쓴다.
- **좌석**: 측벽이 있는 쪽 창가 바는 생략해 진열 코너와 진입 구역을 비운다. 벽면 벤치는 반대쪽 벽 그대로.
- **거부 메시지**: 필요한 폭은 300mm 모듈 단위로 올림해 안내한다(5,200mm 상가에 5,120mm를 요구하던 오류 수정). 중앙 출입구 때문에 측벽을 못 쓰는 경우 “출입구를 왼쪽이나 오른쪽에 두면 해결될 수 있다”고 덧붙인다.
- **브랜드 규칙**: `rules.displayWrap` = `auto | straight`(기본 auto, 구 저장 브랜드는 기본값). 브랜드 표준 편집 탭의 배치 규칙에 [O] 선택으로 노출.

## 스모크 결과 (층고 2,900 · 좌석 포함)

| 상가 | 타입 | 요소 | 측벽 진열 | POS / 음료 / 픽업 | 진열 | 카운터·음료 | 픽업 | 후방 | 객석·여유 | 좌석 | 경고 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 5,000×6,600 (10평) | C | 32~36 | 없음 | 900 / 900 / 900 | 21.3% | 18.2% | 9.1% | 20.8% | 30.6% | 5~6 | POS·음료 900 축소, 벤치 생략 |
| 6,000×8,300 (15평) | C | 50~53 | 3모듈 3,600 | 1,200 / 1,200 / 900 | 21.9% | 16.0% | 6.0% | 16.5% | 39.7% | 7~10 | **없음** |
| 7,200×9,200 (20평) | B | 62~64 | 4모듈 4,200 | 1,200 / 1,800 / 900 | 21.2% | 14.9% | 4.5% | 14.9% | 44.5% | 8~10 | 음료 1,800 확대(안내) |
| 12,000×7,000 (25평 광폭) | B | 59~68 | 없음 | 1,200 / 2,400 / 1,500 | 25.8% | 14.1% | 5.9% | 19.6% | 34.6% | 8~14 | 음료·픽업 확대(안내) |
| 9,000×12,000 (33평 세장) | B | 70 | 4모듈 4,800 | 1,200 / 1,200 / 1,500 | 19.5% | 7.3% ⚠ | 4.6% | 11.4% ⚠ | 57.3% | 10 | 카운터·후방 부족 → 후방 깊이 확대 권고 |
| 3,600×9,000 (세장) | — | 생성 거부 | — | — | — | — | — | — | — | — | “폭 4,620mm 이상 또는 음료·픽업 끄기” |
| 5,200×8,300 중앙 출입구 | — | 생성 거부 | — | — | — | — | — | — | — | — | “5,220mm 이상 … 출입구를 좌/우로 두면 해결” (좌측 출입구는 정상 생성) |

모든 생성 결과는 `collisions()` 0건, `validateScene` 통과, JSON 왕복 동일. 15·20평은 측벽 L자 연장으로 모든 존 목표를 충족한다. 12m 이상 깊은 상가는 직선 라인 + 측벽 하나로는 카운터·후방 존이 부족하며 리포트가 그대로 표시하고 후방 깊이 확대를 권한다.

## 기존 테스트에서 바꾼 단언 (의도된 동작 변경)

| 검사 | 이전 | 현재 |
|---|---|---|
| OFD 기본 표준 | 소재 7개, 노트에 ‘공식 시공 표준’ | 소재 8개, ‘매뉴얼 v1.0’ 포함, 카운터 1,050·플린스 900/800·규칙값 확인 |
| 스키마 거부 | 모듈 NaN 등 | 규칙 범위·존 min>max·미지 키 거부, 구 JSON 정규화 확인 추가 |
| 마감만 교체 | 안쪽 벽 `#1C304A`(네이비) | `#5A3A2A`(월넛), 리포트 없음 |
| 빈 상가 교체 | 요소 9개 | 요소 32개, 출입구·좌석·리포트 기록 |
| 10/15/20평 프리셋 | 9개·그룹 3·‘주문·픽업 상판’ 2,600 | 32/43/49개·그룹 7/9/10·‘카운터 상판 (석재)’ 900×30, 상판 높이 1,050, 3,000K |
| 제약 | 벤치 1,500 → 예외, 깊이 4,000 → 6개 | 벤치 1,500 → 15평에서 1,500 벤치 생성, 깊이 4,000 → 대기열 규칙으로 거부, 좌석 끄기 → 24개 |

## 검증

- `npx tsc --noEmit --incremental false` 통과.
- `node --test tests/*.test.mjs`: **56개 파일 / 757개 검사 통과** (이전 55개 / 740개 + 신규 17개).
- 신규 `tests/brand-studio.test.mjs`(17개): 측벽 L자 연장(15평 3모듈·미러링·진입 구역 이격·리포트), 직선 규칙 재현, 중앙 출입구 거부 메시지, 폭 확대, 프리셋×출입구 9조합 충돌·경계·존 합계·동선·좌석, 존 띠 연속성, 미러링(문 x ±1,690·쇼케이스·벤치 위치), 주통로 장애물 검사, 좌석 끄기, 세장 상가 거부 메시지와 테이크아웃 구성 복구, 서비스 폭 축소 조건, 규칙 오버라이드(대기열 3,000·3,500K·문 1,200·주통로 1,500), 플린스 고정 사양·모듈 채우기, 타입 분류, 체크리스트 전달, 결정성, 마감 모드 무영향·스키마 거부.
- ESLint: 저장소 기준선이 99개 오류로 게이트가 아니다. 변경 파일 4개는 기존과 동일한 `<img>` 경고 1개만 남는다.
- 브라우저 WebGL 렌더링과 실제 D1 저장은 이 환경에서 실행하지 않았다(브랜드 CRUD·프로젝트 저장은 실제 SQLite 시험 대역으로 검증).

## 한계와 다음 단계

- 서비스 라인 + 출입구 쪽 측벽 한 곳까지 생성한다. 양쪽 측벽 연장, 아일랜드 진열, 기둥·비정형 평면, 화장실·창고 구획은 미지원.
- 3D 형상은 실치수 프리미티브 조합이다. 쇼케이스·에스프레소 머신·제빙기 등 실제 모델 형상과 개별 치수는 본사 지정 모델 확보 후 교체한다.
- 리포트의 존 면적은 띠 모델 계산값이며 시공 도면의 존 경계와 다를 수 있다. 공조 취출 위치·컬러 면적비·조도·민화 그래픽은 현장 확인 항목으로만 표시한다.
- 후속 로드맵: ② 2D 평면 드래그 배치(스냅·통로폭 실시간) ③ 상담용 렌더 프리셋(조명·카메라·환경) ④ OFD 견적 연동(소개서 확정치 평당 300만 + 집기·설비 2,500만, 매뉴얼 18개 공정 분리) ⑤ 5단계 위저드 간편 모드.
